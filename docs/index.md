# italic-forge Documentation

italic-forge is a foundation for future optical oblique and semi-automatic italic
generation from OpenType fonts.

The first public surface is:

```bash
forge info path/to/font.ttf
```

Generation APIs are intentionally deferred until the geometric, optical, naming, and export
contracts are designed carefully.
